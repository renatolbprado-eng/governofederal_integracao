{"jsonrpc": "2.0", "id": 1, "method": "initialize", "params": {"protocolVersion": "2024-11-05", "capabilities": {}, "clientInfo": {"name": "test", "version": "1.0.0"}}}
{"jsonrpc": "2.0", "id": 2, "method": "tools/call", "params": {"name": "execute_luau", "arguments": {"code": "local results = {}; local basedrone = nil; for _, obj in pairs(workspace:GetDescendants()) do if obj:IsA('BasePart') and obj.Name:lower() == 'basedrone' then basedrone = obj break end end; local partsToExamine = workspace:GetDescendants(); for _, obj in pairs(partsToExamine) do if obj:IsA('BasePart') then local name = obj.Name:lower(); local isLeg = false; if name:match('perna') or name:match('leg') or name:match('pe') or name:match('foot') or name == 'basedrone' then isLeg = true end; if not isLeg and basedrone and obj ~= basedrone then local dist = (obj.Position - basedrone.Position).Magnitude; if dist < 10 then isLeg = true end end; if isLeg then table.insert(results, obj) end end end; local out = {}; for _, part in pairs(results) do local pName = part.Parent and part.Parent.Name or 'nil'; table.insert(out, 'Name: ' .. part.Name .. ' | Class: ' .. part.ClassName .. ' | Parent: ' .. pName .. ' | Pos: ' .. tostring(part.Position) .. ' | Size: ' .. tostring(part.Size) .. ' | CFrame: ' .. tostring(part.CFrame)) end; return table.concat(out, '\\n');"}}}
